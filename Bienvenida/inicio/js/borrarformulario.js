function borrarformulario() {
    
  
    if (confirm("¿Estás seguro que deseas borrar el formulario " + formulario + "?")) {
      // Aquí va la lógica para borrar el formulario
      alert("El formulario ha sido borrado exitosamente");
      regresar();
    }
  }
  